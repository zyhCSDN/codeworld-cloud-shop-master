server:
  port: 2002
spring:
  application:
    name: codeworld-cloud-logistics
  main:
    allow-bean-definition-overriding: true
  profiles:
    active:
      - config
    include:
      - config
      - db
      - mybatis
      - nacos
      - rabbitmq
      - redis
